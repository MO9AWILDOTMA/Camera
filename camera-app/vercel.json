{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install --save-dev typescript @types/node && npm install"
      }
    }
  ]
}
